# Seats — 教室座位分配管理工具

> 一个现代化的、注重隐私的教室座位分配工具。在浏览器中完成学生座位分配、过道管理、批量操作和 Excel 导出，无任何第三方追踪。
>
> **Warning**：由于作者本人前端水平约等于一只刚学 HTML/CSS/JS 的鸽子在写 React，所以项目里塞了大量 AI 代码。如果你在代码里发现了什么离谱操作，那大概是 AI 写的，不关我事（逃

**[English](./README.md) | 中文**

---

## 功能特点

| 功能              | 说明                                             |
| ----------------- | ------------------------------------------------ |
| **点击式分配**    | 点击姓名，再点击空格，即可填入。也可一键随机分配 |
| **过道管理**      | 将整列标记为过道，被清退的学生自动归还名单       |
| **交换/移除模式** | 两种独立模式，视觉反馈清晰，操作安全             |
| **批量管理**      | 框选或双击即可批量启用/禁用座位                  |
| **Excel 导出**    | 将座位表导出为 `.xlsx` 文件，直接打印使用        |
| **离线可用**      | 纯前端 Svelte 应用，首次加载后可离线使用         |
| **隐私优先**      | 零第三方追踪，无需服务器                         |

---

## 项目结构

```
seats/
├── src/
│   ├── lib/
│   │   ├── components/
│   │   │   ├── ColumnControls.svelte   # 列控制按钮（过道/普通切换）
│   │   │   ├── FileUpload.svelte        # 文件上传 + 随机填入 + 导出
│   │   │   ├── NameList.svelte          # 学生名单列表（可选择）
│   │   │   └── SeatGrid.svelte          # 10×10 座位网格表格
│   │   ├── stores.svelte.ts             # SeatGridState 类（所有网格逻辑）
│   │   ├── types.ts                     # TypeScript 类型定义
│   │   ├── export.ts                    # Excel 导出（基于 ExcelJS）
│   │   └── index.ts                     # 库入口文件
│   ├── routes/
│   │   ├── +layout.svelte               # 根布局
│   │   ├── +layout.ts                  # 预加载配置
│   │   └── +page.svelte                # 主页面（状态编排）
│   ├── app.css                         # 设计令牌 & 全局样式
│   ├── app.html                        # HTML 模板
│   └── app.d.ts                        # TypeScript 声明
├── static/
│   ├── names.txt                       # 默认学生名单（可选）
│   ├── CNAME                           # 自定义域名配置
│   ├── robots.txt                      # 爬虫配置
│   └── .nojekyll                       # GitHub Pages Jekyll 跳过配置
├── svelte.config.js                    # SvelteKit 配置
├── vite.config.ts                      # Vite 构建配置
├── tsconfig.json                       # TypeScript 配置
└── package.json                        # 依赖和脚本
```

---

## 使用说明

### 座位网格

主网格显示 **10×10** 的座位表格，每个单元格可能处于以下状态：

| 状态       | 视觉效果  | 说明             |
| ---------- | --------- | ---------------- |
| **空座位** | 白色/浅色 | 可分配           |
| **已填充** | 蓝色调    | 已有学生姓名     |
| **已禁用** | 灰色      | 永久不可用       |
| **过道**   | 深色整列  | 表示过道，无座位 |

### 模式切换

点击侧边栏中的模式切换按钮，可在两种模式间切换：

- **填座**：用于分配和交换学生
- **移除**：用于从座位移除学生

### 分配学生

1. 点击**右侧名单**中的学生姓名，将其选中
2. 点击网格中任意**空单元格**，该学生即被分配到该座位
3. 分配成功后，姓名从名单中消失

### 交换两个学生

1. 在**填座**模式下，点击一个**已填充单元格** — 该座位高亮显示"交换选中"状态
2. 点击另一个**已填充单元格**，两位学生互换位置
3. 短暂的闪烁动画确认交换完成

### 移除学生

1. 切换到**移除**模式（底部出现红色下划线提示）
2. 点击一个**已填充单元格** — 该座位变红提示确认
3. 再次点击以确认移除，学生姓名回到名单

### 管理过道

1. 点击网格上方的列按钮，可将该列切换为**过道**
2. 过道内如有学生，会被自动清退并归还到名单
3. 列按钮变深色表示该列已设为过道

### 批量启用/禁用座位

**禁用座位**（在填座模式下且未选中姓名时）：

1. 点击一个**空单元格**开始框选
2. 点击另一个单元格确定矩形范围，所有空单元格高亮
3. 再次点击确认，将所有高亮单元格**设为禁用**

**启用座位**：

1. 点击一个**已禁用单元格**，高亮显示
2. 点击另一单元格确定范围
3. 再次点击确认，将所有高亮单元格**设为启用**

### 随机填入

点击侧边栏中的**随机填入**，可将名单中的所有学生一键随机分配到空白座位。

### 导出 Excel

点击**导出 Excel**，将当前座位表下载为 `.xlsx` 文件。

---

## 技术栈

- **框架**: SvelteKit (Svelte 5)
- **语言**: TypeScript
- **构建工具**: Vite
- **样式**: CSS 设计令牌
- **Excel 导出**: ExcelJS

> 说起来你可能不信，这个项目的 CSS 是我（指 AI）和作者一起肝出来的，所以别问为什么某些地方样式写得那么奇妙（目光躲闪

---

## 快速开始

```sh
pnpm install
pnpm dev
```

## 开发命令

| 命令           | 说明                 |
| -------------- | -------------------- |
| `pnpm dev`     | 启动热重载开发服务器 |
| `pnpm check`   | 类型检查和代码检查   |
| `pnpm format`  | 格式化代码           |
| `pnpm build`   | 构建生产版本         |
| `pnpm preview` | 本地预览生产版本     |

构建产物为纯静态文件，可部署到任意静态托管服务。

---

## 许可证

本项目采用 **GNU Affero 通用公共许可证 v3.0**（AGPL-3.0-or-later）。
详见 [LICENSE](LICENSE) 文件。

版权所有 (C) 2026 Una
